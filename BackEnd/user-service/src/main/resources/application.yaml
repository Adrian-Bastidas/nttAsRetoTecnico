server:
  port: 8081

spring:
  profiles:
    active: dev
  application:
    name: user-service

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
